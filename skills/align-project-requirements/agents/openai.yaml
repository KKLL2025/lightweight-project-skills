interface:
  display_name: "Align Project Requirements"
  short_description: "Align materially unclear project requirements"
  default_prompt: "Use $align-project-requirements only when material outcome, scope, delivery depth, constraints, or trade-offs are unclear; otherwise execute the clear task directly."
