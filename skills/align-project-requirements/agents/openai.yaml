interface:
  display_name: "Align Project Requirements"
  short_description: "Align outcomes, sequence, trust, and acceptance"
  default_prompt: "Use $align-project-requirements to align this change, its first executable outcome, and only the material boundaries."
