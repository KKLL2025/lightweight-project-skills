interface:
  display_name: "Drive Large Projects"
  short_description: "Keep multi-session projects adaptive and resumable"
  default_prompt: "Use $drive-large-project only when persistent coordination is useful; continue through bounded execution while preserving only the state needed to resume."
